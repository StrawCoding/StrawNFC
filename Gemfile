source "https://rubygems.org"

# Play CI uses Ruby 3.3 (see .github/workflows/play-internal-release.yml).
# fastlane → excon 1.7+ requires ruby >= 3.3.0; do not pin workflow to 3.2.
ruby ">= 3.3.0"

gem "fastlane"
